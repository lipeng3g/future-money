# Next Task（worker 更新）

## 刚完成
- 已继续把“恢复全部账户”确认框从抽象风险提示推进到可直接判断后果的恢复 diff：
  - `buildImportRiskSummary` 会根据备份作用域（all / legacy-unknown）和当前本地数据规模生成风险级别、后果说明、替换范围
  - `buildImportAccountDiffSummary` 会展示恢复后会新增 / 移除 / 保留的账户名，帮助快速发现“某个本地账户会不会丢”
  - `buildImportDataDeltaSummary` 会展示账户 / 事件 / 对账 / 账本 / 覆盖记录的恢复前后净增减，方便在确认前判断这份备份是否明显偏少或偏多
  - 已补 import-preview 回归测试覆盖高风险 / legacy 未标记文案、账户差异摘要与数据规模变化摘要

## 下一轮优先级
1. **继续压包**：当前构建已消除默认 500kB 告警，但 `chart-base` 共享异步块仍约 471.8kB，可继续按 tooltip / markLine / focus insight 逻辑拆分，进一步降低余额图首次打开成本
2. **图表体验继续深化**：在“图表点 → 事件清单高亮”与“事件清单 → 图表日期”闭环基础上，继续补“图上多事件日期的账户分组说明”或“事件定位后筛出该事件全部发生日”
3. **导入恢复防误操作**：账户名级 diff 与数据规模净增减已落地；下一步可考虑把“事件/对账/账本 diff”继续细化到按账户或按时间范围的摘要，而不只停留在总量变化
4. **AI 体验**：继续检查分析抽屉的流式渲染/大段 markdown 性能，必要时做消息虚拟化或渲染节流
5. **测试面扩展**：既然 `@vue/test-utils` 已接入，可以继续给导入恢复确认流、账户管理高风险操作、图表快速定位条等关键交互补组件级回归，而不只停留在纯函数测试
