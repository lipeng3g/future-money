# Next Task（worker 更新）

## 刚完成
- 已强化 `storage.importState()`：导入/恢复时会自动净化状态，剔除断裂引用、跨账户脏数据与重复 ID，避免坏备份把账本链路污染进本地
- 已补 `src/utils/__tests__/storage.test.ts` 回归用例，锁定“结构合法但引用断裂”的导入文件只保留有效账户/事件/对账/账本/覆盖记录
- 仍保留此前的 FileReader 失败处理与撤销导入闭环能力

## 下一轮优先级
1. **导入/恢复 UI 端到端闭环**：补“坏备份 + 用户确认 + 导入完成 + 撤销回滚”的组件级回归，验证 UI 提示与 store 净化结果一致
2. **字段级数据校验**：继续给导入路径加更细的结构/值校验，例如非法日期、异常 recurrence 组合、非数值余额/金额
3. **运行时 smoke 自动化**：启动预览服务后跑一轮真实导入-撤销操作，减少目前偏单元测试的盲区
4. **AI 体验余项**：继续打磨手动停止生成、超时提示与 reopen 场景，避免流式分析体验抖动
