# Next Task（worker 更新）

## 刚完成
- 已为事件表单补上“接下来会这样发生”的规则预演：
  - `buildEventSchedulePreview` 会复用 recurrence 规则，直接推演最近几次真实发生日期
  - monthly 31 日会在短月展示真实落点（如 2/28），yearly 2/29 会明确展示平年回退到 2/28
  - 仅当规则有效且启用时展示预演，避免把非法配置渲染成误导性结果
  - 已补 event-form 回归测试覆盖 monthly / yearly / disabled / invalid 几条关键边界

## 下一轮优先级
1. **事件表单继续深化**：补组件级交互测试，覆盖频率切换、预演区块显示/隐藏、以及 yearly 无效组合时 help/提交提示的实际 UX
2. **继续压包**：当前构建已消除默认 500kB 告警，但 `chart-base` 共享异步块仍约 471.8kB，可继续按 tooltip / markLine / focus insight 逻辑拆分，进一步降低余额图首次打开成本
3. **图表体验继续深化**：在“图表点 → 事件清单高亮”与“事件清单 → 图表日期”闭环基础上，继续补“图上多事件日期的账户分组说明”或“事件定位后筛出该事件全部发生日”
4. **导入恢复防误操作**：继续打磨 `scope=current|all|legacy-unknown` 的恢复提示，考虑在确认框中增加更显眼的风险分级文案
5. **AI 体验**：继续检查分析抽屉的流式渲染/大段 markdown 性能，必要时做消息虚拟化或渲染节流
