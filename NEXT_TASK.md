# Next Task（worker 更新）

## 刚完成
- 已把“恢复全部账户”确认框升级为更显眼的风险分级确认：
  - `buildImportRiskSummary` 会根据备份作用域（all / legacy-unknown）和当前本地数据规模生成风险级别、后果说明、替换范围
  - 整库备份会明确提示“当前浏览器里已有多少账户/事件/对账/账本/覆盖记录将被替换”
  - 旧版未标记备份不再与正式整库备份混在一起展示，而是单独标记为“中风险”并强调先核对账户数量/时间
  - 已补 import-preview 回归测试覆盖高风险整库恢复与 legacy 未标记备份两类文案策略

## 下一轮优先级
1. **事件表单继续深化**：补组件级交互测试，覆盖频率切换、预演区块显示/隐藏，以及起止/发生日期字段级即时错误提示的实际 UX
2. **继续压包**：当前构建已消除默认 500kB 告警，但 `chart-base` 共享异步块仍约 471.8kB，可继续按 tooltip / markLine / focus insight 逻辑拆分，进一步降低余额图首次打开成本
3. **图表体验继续深化**：在“图表点 → 事件清单高亮”与“事件清单 → 图表日期”闭环基础上，继续补“图上多事件日期的账户分组说明”或“事件定位后筛出该事件全部发生日”
4. **导入恢复防误操作**：继续观察是否还需要把“当前本地数据 vs 备份数据”的差异（新增/删除账户名）做成更直观 diff
5. **AI 体验**：继续检查分析抽屉的流式渲染/大段 markdown 性能，必要时做消息虚拟化或渲染节流
