# Next Task（worker 更新）

## 刚完成
- 已先吸收并保留用户在 `origin/main` 上的两次构建修复：`909d43c` 把 `@ant-design/icons-vue` 并回 antd 主 chunk，`2f918d8` 进一步把所有 `ant-design-vue` / `@ant-design/icons-vue` 合并为单一 `vendor-antd`，用来彻底消除子 chunk 间的 ESM 循环依赖与运行时未初始化崩溃
- 账户管理里的“恢复全部账户 / 导入当前账户”现在都对 `FileReader.onerror` 做了显式处理：读取失败会立刻提示对应错误，并统一重置文件导入模式与 input 状态，不会残留在错误模式里影响下一次导入
- 已扩展 `src/layouts/__tests__/AppHeader.test.ts`，覆盖整库恢复与当前账户导入两条“文件读取失败”分支，确保不会误弹确认框，也不会因为状态未复位而串到下一次操作

## 下一轮优先级
1. **AI 体验与稳定性继续收口**：本轮已补“关闭/切 scope 时主动 abort + 过期结果不写回”，下一轮可继续看“手动停止生成”“请求超时提示”“streaming 中再次打开/关闭抽屉”的细节体验
2. **图表性能余项**：当前 `chart-balance-runtime` 仍约 556kB；但涉及构建/拆包策略时要继续尊重用户已确认的 ant-design-vue 修复边界，优先看余额图内部能力按需装载，而不是再动 antd chunk 策略
3. **继续保守打磨导入/恢复稳定性**：可继续补“确认文案输入错误后重试”“取消后重新选择文件”“同一次会话连续两次失败/成功交替”的组件级回归，把账户管理高风险文件流补齐闭环
4. **运行时防护**：继续梳理本地导入、恢复、撤销、删除等高风险路径是否还有遗漏的异常分支与 UI 级提示空洞
